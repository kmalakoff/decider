{
  "name": "decider-admin",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "start-development": "npm install && cross-env NODE_ENV=development nodemon -L --harmony-async-await --watch server server/index.js",
    "start": "cross-env NODE_ENV=production node --harmony-async-await server/index.js",
    "compile": "npm install && cross-env NODE_ENV=production webpack --display-modules",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "license": "MIT",
  "devDependencies": {
    "nodemon": "^1.11.0"
  },
  "dependencies": {
    "body-parser": "^1.15.2",
    "cors": "^2.8.1",
    "cross-env": "^3.1.3",
    "express": "^4.14.0",
    "lodash": "^4.17.2",
    "lodash.uniqueid": "^4.0.1"
  }
}
