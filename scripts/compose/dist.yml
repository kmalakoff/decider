version: "2"
services:
  web:
    image: decider/web:dev
    build:
      context: ../../web
      dockerfile: Dockerfile.dev
    volumes:
        - ../../web:/home/nodejs/app
    env_file:
      - environments/web.production.env
    command: npm run dist
